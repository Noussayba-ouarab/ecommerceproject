<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>payment</title>
</head>
<body>
    <!-- <div>
    
    <a href="#" id="market-icon" style="color:  {{$cartCount > 0 ? 'red' : 'black' }}">
        <i class="fa fa-shopping-cart" aria-hidden="true"></i>
        <span class="padding_10" id="item-count">{{ $cartCount }}</span>
    </a>
</div>  -->
<!-- <div>
    <a href="#" id="market-icon">
        <i class="fa fa-shopping-cart" aria-hidden="true"></i>
        <span class="padding_10" id="item-count">
            {{ $cartCount }}
        </span>
    </a>
</div> -->
// if (session(sum > 0)) {
      //     document.getElementById('item-count').style.display = 'inline';
      // } else {
      //     document.getElementById('item-count').style.display = 'none';
      // }
      // $cartData = collect(Session::get('cart', []))->pluck('item')->toArray();
        // $values = array_map('intval', $cartData);
        // $sum = array_sum($values);
        // Session::flash('sum', $sum);
        // $sum = 0;
        // foreach ($cart as $item) {
        //     // Access the total price of each item and add it to the total
        //     $sum = intval($item['item']);
        //     $c[] = $sum;
        // }
        // Session::put('cart', $cart);
        // Session::put('sum', $sum);
        // $cart = Session::get('cart', []);
        // Session::put('cart', $cart);
        // $sum = 0;
        // foreach ($cart as $item) {
        //     // Access the total price of each item and add it to the total
        //     $sum = intval($item['item']);
        //     $c[] = $sum;
        // }
        // //Session::flash('sum', $sum);
        // Session::put('cart', $cart);
        // Session::put('sum', $sum);
        // $cart = Session::get('cart', []);
        // Session::put('cart', $cart);
        // $sum = 0;
        // foreach ($cart as $item) {
        //     // Access the total price of each item and add it to the total
        //     $sum = intval($item['item']);
        //     $c[] = $sum;
        // }
        // //Session::flash('sum', $sum);
        // Session::put('cart', $cart);
        // Session::put('sum', $sum);
</body>
</html>